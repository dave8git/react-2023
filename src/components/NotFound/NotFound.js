import styles from './NotFound.module.scss';
import PageTitle from '../PageTitle/PageTitle';

const NotFound = () => {
    return (
        <PageTitle>It's 404 Bro! There is nothing here!</PageTitle>
    );
};

export default NotFound;